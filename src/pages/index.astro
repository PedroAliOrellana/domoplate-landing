---
import MainLayout from '../layouts/MainLayout.astro';
import Hero from '../components/Hero.astro';
import MiniCalculadora from '../components/MiniCalculadora';
import LeadForm from '../components/LeadForm';
---

<MainLayout title="Domoplate - Gestión Inteligente para Restaurantes">
  
  <Hero 
    headline="Domina tu cocina, escala tu negocio"
    subhead="La plataforma todo-en-uno para restaurantes y dark kitchens que buscan eficiencia."
  />

  <main class="flex flex-col items-center w-full">
    
    <!-- Calculator Section -->
    <section id="calculator" class="w-full py-16 px-4 bg-background">
      <div class="max-w-4xl mx-auto flex flex-col md:flex-row items-center gap-12">
        <div class="flex-1 space-y-4">
          <h2 class="text-headline-medium font-bold text-on-surface">¿Cuánto dinero estás dejando en la mesa?</h2>
          <p class="text-body-large text-on-surface-variant">
            Pequeñas ineficiencias se suman. Usa nuestra calculadora para ver el potencial de ahorro mensual que Domoplate puede desbloquear para tu negocio.
          </p>
        </div>
        <div class="flex-1 w-full flex justify-center">
          <MiniCalculadora client:visible />
        </div>
      </div>
    </section>

    <!-- Lead Form Section -->
    <section id="lead-form" class="w-full py-20 px-4 bg-secondary-container text-on-secondary-container rounded-t-[32px] md:rounded-t-[48px] -mt-8 z-10">
      <div class="max-w-xl mx-auto text-center space-y-8">
        <div>
          <h2 class="text-headline-medium font-bold mb-2">Empieza tu transformación hoy</h2>
          <p class="text-body-large text-on-secondary-container/80">
            Déjanos tus datos y recibe una consultoría inicial gratuita.
          </p>
        </div>
        
        <div class="bg-surface p-6 md:p-8 rounded-2xl shadow-lg border border-outline/10 text-left">
          <LeadForm client:load /> // Use client:load to ensure interactivity
        </div>
      </div>
    </section>

  </main>
</MainLayout>